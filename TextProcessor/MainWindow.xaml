<Window x:Class="TextProcessor.Views.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		Title="Text Processor" Height="400" Width="600">
	
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0"
			Text="Select a file to count word occurrences."
			Margin="5"
			TextWrapping="Wrap"
			HorizontalAlignment="Center"/>
		
		<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5" >
			<Button Content="Select file" Click="OnSelectFileClick" Padding="10,5"/>
		</StackPanel>

		<DataGrid x:Name="ResultsDataGrid" Grid.Row="2" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
		  AutoGenerateColumns="False" CanUserAddRows="False" Margin="10">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Word" Binding="{Binding Word}" SortMemberPath="Word" Width="*"/>
				<DataGridTextColumn Header="Count" Binding="{Binding Count}" SortMemberPath="Count" Width="Auto"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</Window>